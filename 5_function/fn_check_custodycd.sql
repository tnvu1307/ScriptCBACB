SET DEFINE OFF;
CREATE OR REPLACE FUNCTION fn_check_custodycd(P_CUSTODYCD VARCHAR2) RETURN VARCHAR2 IS

BEGIN
    IF LENGTH(P_CUSTODYCD) < 10 THEN
        RETURN '-1';
    END IF;
    RETURN '0';
EXCEPTION WHEN OTHERS THEN
    RETURN '-1';
END fn_check_custodycd;
/
