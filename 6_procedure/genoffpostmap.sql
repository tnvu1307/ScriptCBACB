SET DEFINE OFF;
CREATE OR REPLACE PROCEDURE genoffpostmap
   IS
BEGIN
	FOR REC IN (SELECT TXNUM, TO_CHAR(TXDATE,'DD/MM/YYYY') TXDATE FROM TLLOG)
	LOOP
		AUTOGENPOSTMAP(REC.TXDATE,REC.TXNUM);
	END LOOP;
EXCEPTION
   WHEN OTHERS THEN
    RETURN ;
END;

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
/
